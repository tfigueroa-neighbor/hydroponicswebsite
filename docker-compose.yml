version: '3'

services:
  wordpress:
    image: hydroponics:latest
    container_name: hydroponics_wordpress
    volumes: 
      - ./wordpress/html:/var/www/html
    networks:
      - wpsite

networks:
  wpsite:
    internal: true